//TODO - Class not used as of 7/17/23 - Will revisit once this page makes it to the merge app

/*package com.DC.pageobjects.retail.salesTrends;

import com.DC.pageobjects.PageHandler;
import org.apache.log4j.Logger;
import org.openqa.selenium.WebDriver;

import com.DC.utilities.ReadConfig;

public class GainersDrainersPage extends PageHandler {

    private Logger logger;
    private CommonMethods common;
    private LoginPage lp;
    private ReadConfig readConfig;

    public GainersDrainersPage(WebDriver rdriver) {
        super(rdriver);
        common = new CommonMethods(rdriver);
        lp = new LoginPage(rdriver);
        readConfig = ReadConfig.getInstance();
        logger = Logger.getLogger("fila");
    }

    public static String chartTitle = "//h5[contains(text(), 'Gainers & Drainers')]";
    public static String itemInAsinTextBox = "//select[@id='ASINs']/..//div[@class='item']";
}*/

